# Stating

The `Git Index` (also known as staging area or cache) is a critical, intermediate are between your
working directory and you Git repository (where commits are permanently stored). It is a single binary file
located at `.git/index`
